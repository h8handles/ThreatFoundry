from intel.services.threatfoundry_scoring import ThreatFoundryScorer

def build_dashboard_context(filters):
    # Existing code...
    
    for record in ordered_queryset.iterator():
        record.calculated_score = scorer.calculate_score(record.to_dict())
        record.score_breakdown = scorer.get_breakdown()
    
    return context
