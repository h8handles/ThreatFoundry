## Weekly Bug Checklist

### Monday
- [X] Remove or justify the duplicate WHOIS API route in `intel/urls.py`
- [X] Verify only one canonical WHOIS endpoint is used in templates 
        - got rid of the ioc_blade_details.html template seems to be a dupe but put .bak of it in local directory just incase
- [X] Verify only one canonical WHOIS endpoint is used in JS
- [X] Confirm no old route references remain after cleanup
- [X] Manually test IOC detail WHOIS behavior after route cleanup

### Tuesday
- [ ] Update `README.md` to use `localhost:8080` as the main local example
- [ ] Remove or demote the hardcoded `172.30.150.130:8080` example in docs
- [ ] Add a clear note that wildcard hosts in debug are for local development only
- [ ] Recheck Quick Start steps for anything stale or misleading

### Wednesday
- [ ] Move `import markdown` from inside `documentation_view()` to module level in `intel/views.py`
- [ ] Clean up import ordering in `intel/views.py`
- [ ] Do a quick pass for other lazy imports that should be top level
- [ ] Run the docs page manually to confirm nothing broke

### Thursday
- [ ] Add a smoke test for `intel:generate_exec_report`
- [ ] Test normal HTML report response
- [ ] Test `?format=markdown` response
- [ ] Confirm reverse lookup for the route works and stays covered

### Friday
- [ ] Add a smoke test for `/docs/`
- [ ] Add a test for invalid documentation page returning 404
- [ ] Verify dashboard still uses `intel:dashboard` consistently
- [ ] Check whether `dashboard_alias` is still needed or just legacy clutter

### Manual Verification Pass
- [ ] Load dashboard
- [ ] Load IOC detail page
- [ ] Load aggregated IOC blade page
- [ ] Load docs page
- [ ] Load executive report page
- [ ] Export dashboard CSV
- [ ] Check browser console for frontend errors
- [ ] Note any slow page loads, especially IOC detail with WHOIS enabled

### Documentation Drift Check
- [X] Confirm every route listed in `README.md` still exists (handled by /intel/management/drift_detector.py)
- [ ] Confirm every management command listed in `README.md` still exists
- [ ] Confirm documented env vars are still actually used
- [X] Remove any stale references from old features or old paths
        Route Drift Detection Report
        ============================
        ✅ /auth/login/ exists
        ✅ /auth/register/ exists
        ✅ /auth/logout/ exists (POST only)
        ✅ / exists
        ✅ /dashboard/ exists
        ✅ /assistant/ exists
        ✅ /api/assistant/chat/ exists (POST only)
        ✅ /api/assistant/context/ exists (POST only)
        ✅ /docs/ exists
        ✅ /docs/some-doc/ exists
        ✅ /malware/ exists
        ✅ /ioc-blade/ exists
        ✅ /ioc/1/ exists
        ✅ /admin/ exists


### Stretch Items If Time Remains
- [ ] Decide whether synchronous WHOIS lookup on IOC detail is acceptable
- [ ] Standardize internal URL usage to one dashboard route name
- [ ] Look for other low risk cleanup opportunities in `intel/views.py`
